{
  "_comment": "Example only. Add this to any MCP-compatible client and replace URL/TOKEN with your own values.",
  "mcpServers": {
    "remote-ops": {
      "url": "http://YOUR_RELAY_MCP_SERVER:3001/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_JWT_TOKEN"
      }
    }
  }
}
